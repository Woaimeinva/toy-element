// components.ts

import { ErButton, ErBox, ErIcon, ErButtonGroup, ErAlert, ErCollapse, ErCollapseItem, ErMarquee, ErShake, ErWatermark } from "@toy-element/components";
import type { Plugin } from "vue";

export default [ErButton, ErBox, ErIcon, ErButtonGroup, ErAlert, ErCollapse, ErCollapseItem, ErMarquee, ErShake, ErWatermark] as Plugin[];
